### File 2: `Setup.md`
```markdown
### 🗄️ Database Attack Simulation Testbed Configuration

To safely audit input vulnerabilities, track query behavior, and execute authentication overrides without impacting operational infrastructure networks, the assessment profile was restricted to an isolated testing ecosystem:

* **Vulnerable Endpoint Architecture:** Web application environment optimized with active input-sanitization vulnerabilities specifically engineered to model dynamic database query parsing defects.
* **Database Management Engine Profile:** Relational database management structure running structural query processing routines.
* **Vulnerability Target Track:** Secure validation of authentication logic parsing gaps, administrative session hijacking boundaries, and alternative query comment exploitation tracks (e.g., parsing query drops via symbol sequences).
